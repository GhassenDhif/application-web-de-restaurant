{{ form_start(f) }}
                    <label for="inputText" class="col-sm-6 col-form-label">Nom</label>
                    <div class="col-sm-10">
                        {{ form_widget(f.nom,{'attr':{'class':'form-control'}}) }}
                    </div>
                     <label for="inputText" class="col-sm-6 col-form-label">Descreption</label>
                    <div class="col-sm-10">
                        {{ form_widget(f.descp,{'attr':{'class':'form-control'}}) }}
                    </div>
                      <label for="inputText" class="col-sm-6 col-form-label">image</label>
                    <div class="col-sm-10">
                        {{ form_widget(f.image,{'attr':{'class':'form-control'}}) }}
                    </div>
                        <label for="inputText" class="col-sm-6 col-form-label">prix</label>
                    <div class="col-sm-10">
                        {{ form_widget(f.prix,{'attr':{'class':'form-control'}}) }}
                    </div>



                   
                    <div class="col-sm-10" style="display: flex;align-items: center;justify-content: center ">

                        {{ form_widget(f.submit,{'attr':{'class':'btn btn-primary'}}) }}
                    </div>

                    {{ form_end(f) }}
